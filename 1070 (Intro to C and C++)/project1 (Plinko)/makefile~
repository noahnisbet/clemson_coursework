# Makefile for project-1.cpp


# This command will "clean" out the directory of any output files
clean:
	rm *.exe

# This command will compile the source code when you run
compile:
	g++ project-1.cpp -o p1.exe

# This command will run the Program
run:
	./p1.exe
